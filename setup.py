from setuptools import setup, find_packages

setup(
    name= 'src',
    version= '0.0.1',
    description= 'Flight fare price prediction',
    author='reena',
    packages=find_packages(),
    license='MIT'
)